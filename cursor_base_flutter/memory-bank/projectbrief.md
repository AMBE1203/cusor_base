# Project Brief

## Overview

Flutter application workspace (`flutter_cursor_plugin_example` / `cursor_base_flutter`): Material app baseline with FVM-pinned Flutter SDK.

## Goals

- Maintain a reproducible Flutter dev setup (FVM, `pubspec` constraints).
- Use the Memory Bank for task tracking and context across VAN → PLAN → BUILD → REFLECT → ARCHIVE.

## Constraints

- Core Memory Bank files live only under `memory-bank/` (see workspace rules).
